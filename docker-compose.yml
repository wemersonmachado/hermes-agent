version: "3.8"

services:
  hermes-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hermes-agent-gateway
    restart: always
    env_file:
      - .env
    volumes:
      - hermes_data:/opt/data

volumes:
  hermes_data:
    driver: local
